# ── Python cache ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
*.env.local

# ── Benchmark run outputs (large, regeneratable) ──────────────────────────────
# Raw pipeline run logs — can be 50-250 MB each
bench/runs/*.jsonl

# Labeling CSVs — intermediate generated artifacts
bench/labels/*.csv

# RAGAS golden answer sets — generated via Gemini API
bench/golden_*.jsonl

# Combined external query file — regenerated by fetch_external_queries.py
bench/queries_external.jsonl

# ── Local archive (old intermediate files kept for local reference only) ──────
_archive/

# ── External cloned repos ─────────────────────────────────────────────────────
code-rag-bench/

# ── Junk / scaffolding dirs ───────────────────────────────────────────────────
_pruned_from_root/
Thesis_Proj_benchmarking_empty_shell/

# ── IDE / tool settings ───────────────────────────────────────────────────────
.claude/
.vscode/
.idea/
*.code-workspace

# ── Large binary / PDF files ─────────────────────────────────────────────────
*.pdf

# ── OS noise ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Pytest cache ──────────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
